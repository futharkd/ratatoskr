[server]
listen_addr = "127.0.0.1:8080"

[defaults]
replay_tolerance_seconds = 300
http_timeout_seconds = 10
max_retries = 3
retry_backoff_millis = 300

[storage]
backend = "sqlite"
sqlite_path = "./ratatoskr.db"

# PostgreSQL alternative:
# backend = "postgres"
# postgres_url = "postgres://ratatoskr:ratatoskr@postgres:5432/ratatoskr"

[includes]
# Hybrid loading:
# 1) convention folders are auto-loaded:
#    examples/config/providers/*.toml
#    examples/config/profiles/*.toml
#    examples/config/services/*.toml
# 2) explicit globs can be added per section:
providers = ["config/providers/*.toml"]
security_profiles = ["config/profiles/*.toml"]
services = ["config/services/*.toml"]
